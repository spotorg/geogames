import styled from "styled-components/native";

const SignUpButtonText = styled.Text`
    color: #000000;
    font-size: 20px;
`;

const SignInButtonText = styled.Text`
    color: #ffffff;
    font-size: 20px;
`;

const TitleScreenBottom = styled.Text`
    color: #000000;
    font-size: 14px;
    position: absolute;
    bottom: 20px;
    left: 0;
    width: 100%;
    text-align: center;
`;

const LoadingScreenTitle = styled.Text`
    color: #000000;
    font-size: 26px;
`;

const WhiteText = styled.Text`
    color: #ffffff;
    font-size: 16px;
`;

const BlackText = styled.Text`
    color: #000000;
    font-size: 18px;
`;

const LinkText = styled.Text`
    color: #5858eb;
    font-size: 16px;
`;

const Label = styled.Text`
    color: #000000;
    font-size: 14px;
    margin-bottom: 5px;
`;

const OptionTextBlack = styled.Text`
    color: #000000;
    font-size: 18px;
`;

const PopupOptionText = styled.Text`
    color: #000000;
    font-size: 16px;
    margin: 0 0 0 10px;
`;

const CenterText = styled.Text`
    color: #000000;
    font-size: 16px;
    position: absolute;
    top: 50%;
    z-index: 2;
`;

const PopupOptionTextRed = styled.Text`
    color: #d32323;
    font-size: 16px;
    margin: 0 0 0 10px;
`;

export {
    SignUpButtonText,
    SignInButtonText,
    TitleScreenBottom,
    LoadingScreenTitle,
    Label,
    WhiteText,
    BlackText,
    LinkText,
    OptionTextBlack,
    PopupOptionText,
    PopupOptionTextRed,
    CenterText
}